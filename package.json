{
  "name": "qr-maker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/generate-agent-docs.js && wrangler pages dev . --compatibility-date=2024-01-01",
    "build": "node scripts/generate-agent-docs.js",
    "deploy": "node scripts/generate-agent-docs.js && wrangler pages deploy ."
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
